# State

## TODO

- [ ] ERiC version lookup: replace hardcoded version with a proper lookup (e.g. scrape ELSTER developer portal or maintain a version manifest). Current approach just hardcodes the known latest version.
- [ ] GitHub Actions release workflow: create CI that builds Linux/macOS/Windows binaries and creates a GitHub release on each tag push. Draft workflow exists at `.github/workflows/release.yml` but needs testing.
